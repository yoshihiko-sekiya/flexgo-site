{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "build:zip": "pnpm generate && cd .output/public && zip -r ../../flexgo.zip ."
  },
  "dependencies": {
    "nuxt": "^3.17.6",
    "v-gsap-nuxt": "^1.4.4",
    "vue": "^3.5.17",
    "vue-router": "^4.5.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.10",
    "sass-embedded": "^1.89.2"
  }
}
